[package]
name = "app"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "nodeimg"
path = "src/main.rs"

[dependencies]
engine = { path = "../engine" }
gui = { path = "../gui" }
pollster = { workspace = true }
types = { path = "../types" }
wgpu = { workspace = true }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
