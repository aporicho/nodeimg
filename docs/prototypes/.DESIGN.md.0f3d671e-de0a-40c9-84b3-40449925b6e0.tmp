# GUI 设计决策

本文档记录探索分支中的视觉和交互设计决策。

## D1: 画布即主窗口

节点画布占据整个窗口，是唯一的底层组件。工具栏、预览面板、状态栏、节点库、AI 对话等均为浮动面板，叠加在画布之上。

```plaintext
┌─────────────────────────────────────────────┐
│ ┌─ toolbar ──────────────────────────────┐  │
│ └────────────────────────────────────────┘  │
│                                             │
│              节点画布（全窗口）               │
│                                    ┌──────┐ │
│                                    │preview│ │
│                                    │      │ │
│                                    └──────┘ │
│ ┌─ statusbar ────────────────────────────┐  │
│ └────────────────────────────────────────┘  │
└─────────────────────────────────────────────┘
```

不存在固定的分栏布局。所有面板可显隐、可拖拽，画布始终铺满。